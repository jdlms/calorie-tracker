services:
  calorie-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    image: calorie-tracker:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - /opt/data/calorie-tracker:/app/data
    restart: unless-stopped
