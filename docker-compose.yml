services:
  calorie-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    image: calorie-tracker:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
